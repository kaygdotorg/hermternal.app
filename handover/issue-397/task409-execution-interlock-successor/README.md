# Phase A, anchor, replay, and Phase B interlock successor

This speculative checkpoint records the only permitted operation order:
Phase A, create-only external anchor, isolated replay, and genuine Phase B.
It returns a stop marker before each operation. It does not execute an operation.

The runner consumes only the closed final #403 provenance path. It binds the
fixed base, tree, protected-main, integrated, and source identities. It loads
#399 from verified bytes and explicitly installs the verified #400 reviewer in
Phase B. Runtime and replay roots must be distinct canonical paths below
`/private/tmp` and outside the checkout.

Each PASS observation has a closed field set and links to the prior event by
SHA-256. A missing, repeated, reordered, changed, or failed observation stops
resume. Tests mock the future external operation results. They use the real
provenance parser and real verified reviewer installation.

No Phase A, anchor creation, replay, Phase B, candidate shell, Git operation,
network action, credential access, socket access, or Hermes action is part of
this checkpoint.
