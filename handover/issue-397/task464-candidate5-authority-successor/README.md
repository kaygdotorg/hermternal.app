# Candidate-five authority successor

This speculative checkpoint defines JSON as structured authority, Markdown as
exact shell parity, and the CLI as a fresh stdin extraction from the validated
pair. It uses only durable repository files and mock test data. It does not run
Git, replay, a candidate shell, a network operation, credentials, or Hermes.

The checkpoint cannot be integrated or approved before issue #400 passes and
an independent review approves these exact bytes. Candidate-three,
candidate-four, and all frozen handover evidence remain unchanged.

The authority JSON has a closed root and `execution_driver` schema. Its exact
argv and strict Git environment must match the reviewed values. The returned
argv is the validated JSON array, and stdin is the validated JSON shell bytes.
