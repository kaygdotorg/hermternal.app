import '../app.css';

/**
 * W-01 is intentionally client-only. Runtime and authentication routes stay
 * out of this scaffold until the Paper and proof gates are approved.
 */
export const ssr = false;
export const prerender = true;
export const trailingSlash = 'always';
