# Scripts

This directory is reserved for small repository tools.

Future scripts may generate platform token outputs, validate contracts, compare fixtures, and check web/Apple parity. Scripts must be deterministic and must not contact live Hermes services by default.

No executable script or tool dependency exists here yet.
