# Design tokens

This directory will define semantic names for color, typography, spacing, radius, elevation, motion, and interaction states.

The tokens express shared design intent. Future tools may produce CSS variables for the web client and native values for SwiftUI. Platform components remain separate and may adapt values for accessibility, browser zoom, Dynamic Type, contrast, reduced motion, and reduced transparency.

No generated token output exists here yet.
