# Protocol fixtures

This directory will contain redacted request, response, WebSocket, and recovery fixtures for both clients.

Fixtures must represent long sessions, streaming, reasoning, tools, approvals, clarification, interruption, reconnect, loading, empty, success, and failure states. Each fixture must identify its contract version and expected state transitions.

Use synthetic data only. Do not commit credentials, live transcripts, hostnames, tokens, or user data.
